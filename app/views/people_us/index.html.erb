<p id="notice"><%= notice %></p>

<h1>Listing People Us</h1>

<table>
  <thead>
    <tr>
      <th>Tableid</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @people_us.each do |people_u| %>
      <tr>
        <td><%= people_u.tableid %></td>
        <td><%= link_to 'Show', people_u %></td>
        <td><%= link_to 'Edit', edit_people_u_path(people_u) %></td>
        <td><%= link_to 'Destroy', people_u, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New People u', new_people_u_path %>
